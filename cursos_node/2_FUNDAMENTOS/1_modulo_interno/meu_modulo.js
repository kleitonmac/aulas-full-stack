// Exportar um módulo personalizado
// module.exports permite que este arquivo seja utilizado por outro arquivo
// Aqui estamos exportando um objeto com uma função 'soma'

module.exports = {
  // Função que recebe dois parâmetros (a e b) e exibe sua soma
  soma(a, b) {
    console.log(a + b)
  },
}
